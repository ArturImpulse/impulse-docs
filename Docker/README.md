# 🐳 Документация по контейнерам Impulse API

## 📦 Общий список контейнеров

| Контейнер | Образ | Назначение | Порты |
|------------|-------|-------------|--------|
| **impulse-api-php-1** | `yiisoftware/yii2-php:7.4-apache` | Основной backend API (PHP + Apache, приложение Yii2) | `80:80` |
| **impulse-api-database-1** | `mysql:5.7` | База данных MySQL | `3306:3306` |
| **impulse-api-phpmyadmin-1** | `phpmyadmin:latest` | Веб-интерфейс для MySQL | `8081:80` |

---

## 🚀 Вход в контейнер

Для выполнения команд внутри PHP-контейнера:

```bash
docker exec -it impulse-api-php-1 bash
```

После входа ты попадёшь в окружение Linux контейнера (например, `/var/www/html`).

---

## ⚙️ Работа с Yii2

### Применить миграции:

```bash
php yii migrate
```

### Очистить кэш:

```bash
php yii cache/flush-all
```

### Проверить подключение к БД:

```bash
php yii db-info
```

*(если такой команды нет — можно просто подключиться через `mysql` в контейнере)*

---

## 🗄 Подключение к MySQL

### Вход в БД:

```bash
docker exec -it impulse-api-database-1 mysql -u impulse -p
```

(Пароль указывается в `.env` или `docker-compose.yml`)

### Проверить список баз:

```sql
SHOW DATABASES;
```

---

## 🧰 Работа с phpMyAdmin

- Открыть в браузере: [http://localhost:8081](http://localhost:8081)
- Host: `database` или `impulse-api-database-1`
- User: `impulse`
- Password: (см. `.env`)

---

## 🧩 Полезные команды Docker

| Команда | Описание |
|----------|-----------|
| `docker ps` | Показать все запущенные контейнеры |
| `docker-compose up -d` | Запустить проект в фоне |
| `docker-compose down` | Остановить и удалить контейнеры |
| `docker logs impulse-api-php-1` | Просмотр логов PHP |
| `docker restart impulse-api-php-1` | Перезапуск PHP-контейнера |
| `docker exec -it impulse-api-database-1 bash` | Войти в MySQL-контейнер |
